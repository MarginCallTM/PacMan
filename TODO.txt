================================================================================
PACMAN — "Ghosts! More ghosts!" (42 project) — MASTER TODO
================================================================================
Legend: [ ] todo   [~] in progress   [x] done
Team: acombier & Sloubiat (see TEAM SPLIT at the bottom of this file).
Order roughly follows the recommended build order; tasks 2-4 (data layer)
and task 5 (rendering sandbox) can be done in parallel by two people.

--------------------------------------------------------------------------------
0. PROJECT SETUP & TOOLING                                             owner: both (kickoff)
--------------------------------------------------------------------------------
[x] 0.1  Create the git repository (source at root, per subject ch. VII/X)
[x] 0.2  Environment management with uv:
         [x] uv init: pyproject.toml with requires-python >= 3.10
         [x] commit pyproject.toml + uv.lock (reproducible env for both
             teammates and for the peer review)
         [x] .venv/ created by uv -> goes in .gitignore
         [x] document the uv workflow in README Instructions
             (done 2026-07-28 with the README pass, incl. pip fallback)
[x] 0.3  Declare dependencies via uv add: pygame, and dev group:
         flake8, mypy, pytest; add mazegenerator from the provided .whl
         (path/local dependency — verify `uv sync` reinstalls it cleanly
         on a fresh clone, the package is re-installed at peer review)
         NOTE: provided .whl is misnamed (filename version 00001 vs 2.0.2
         in its metadata; pip/uv reject mismatched wheels) -> committed a
         byte-identical copy renamed mazegenerator-2.0.2-py3-none-any.whl;
         the original stays available in git history. Fresh-clone uv sync
         verified OK (relative path in [tool.uv.sources]).
[x] 0.4  Write .gitignore (venv, __pycache__, .mypy_cache, .pytest_cache,
         build artifacts, highscore file if generated)
[x] 0.5  Write the Makefile with mandatory targets:
         [x] install   (uv sync — with a documented pip fallback in case
                        the review machine has no uv)
         [x] run       (uv run python3 pac-man.py config.json)
         [x] debug     (uv run python3 -m pdb pac-man.py config.json)
         [x] clean     (remove __pycache__, .mypy_cache, .pytest_cache,
                        etc. — NOT .venv/uv.lock)
         [x] lint      (uv run flake8 . && uv run mypy . --warn-return-any
                        --warn-unused-ignores --ignore-missing-imports
                        --disallow-untyped-defs --check-untyped-defs)
         [x] lint-strict (optional: flake8 . && mypy . --strict)
         [x] test      (optional: uv run pytest)
         NOTE: flake8 config lives in .flake8 (extend-exclude = .venv);
         flake8 does NOT read pyproject.toml and does NOT skip .venv by
         default.
[x] 0.6  Create the package skeleton (empty modules with docstrings):
         [x] pacman/{__init__.py, config.py, maze_loader.py, highscores.py}
             + pac-man.py (module docstrings added)
         [x] pacman/entities/{__init__.py, player.py, ghost.py, pellets.py}
         [x] pacman/game/{__init__.py, level.py, engine.py, states.py}
         [x] pacman/ui/{__init__.py, renderer.py, menus.py}
         [x] tests/, project-management/
[x] 0.7  Verify `make lint` passes on the empty skeleton (pipeline green
         from day one — no "we'll clean it later")
         (2026-07-16: flake8 + mypy green on the full skeleton + config.py;
         fixed W292 missing final newline on all 16 skeleton files)
[x] 0.8  SUBJECT v1.5 UPDATE (2026-07-16): graphical library rule hardened —
         every function used must have an MLX equivalent. pygame.draw.*,
         mixer, alpha, rotations are now FORBIDDEN. Rendering must be
         framebuffer-style (per-pixel writes into a buffer + blit + text +
         key hooks only). See CLAUDE.md "Graphics Library Rule".
         RESOLVED 2026-07-17: reference = school MLX "mlx_CLXV v2.2"
         (real mlx.h + official Python ctypes wrapper), local copy in
         project-management/reference/minimlx/ (NOT committed - school
         code, reference only). Decision: pygame-framebuffer, every
         pygame call mapped 1:1 to a real mlx.h function (mapping table
         to go in README, task 13.8/13.9). Extra allowances found in the
         real API vs our assumptions: mouse hooks, PNG/XPM image loading,
         clear window, screen size, key autorepeat ctrl, loop_exit.
         Still forbidden: draw.*, mixer/sound, rotation, alpha, and
         pygame.time (MLX has no clock -> timing via stdlib `time`).
         SUBJECT UPDATE (2026-07-20): the subject now MANDATES MLX
         itself — the pygame-framebuffer plan (and its README mapping
         table, old 13.8/13.9 note) is DROPPED. We use the school MLX
         "mlx_CLXV v2.2" (C/X11 lib + official Python ctypes wrapper),
         tarball in project-management/reference/minimlx/. Tasks 0.9,
         5, 8.8 and 11 updated below; data layer & game logic (2-4,
         6-8) are graphics-free by design -> unaffected.
[x] 0.9  MLX switch cleanup: pygame dropped from pyproject/uv.lock
         (Sloubiat, merged 2026-07-22); MLX ships as
         mlx-2.2-py3-none-any.whl committed at repo root
         ([tool.uv.sources], same pattern as mazegenerator).
         NOTE: libmlx.so inside the wheel is a Linux/X11 ELF binary
         -> the game window only runs on Linux; on acombier's macOS
         Mlx() raises OSError but imports/lint/pytest all work.
         Packaging (task 11) must state the Linux/X11 requirement.

--------------------------------------------------------------------------------
1. ENTRY POINT: pac-man.py                                             owner: Sloubiat
--------------------------------------------------------------------------------
[x] 1.1  Argument parsing: EXACTLY one argument (the JSON config file);
         wrong count -> usage message + clean exit (no traceback)
         (_parse_args: len(argv) != 2 -> usage to stderr + SystemExit(1))
[x] 1.2  Top-level error guard: try/except around the whole program so ANY
         unhandled error prints a clear message, never a Python traceback
         (__main__ guard: KeyboardInterrupt -> exit 130, Exception ->
         "error: unexpected failure: ..." + exit 1, no traceback)
[x] 1.3  Wire together: load config -> load highscores -> start engine
         (DONE by 2026-07-29 (link ui to engine) and settled since:
         main() loads config+highscores, run_app() builds the MLX
         window + every screen once, a single loop hook (frame()) and
         a single key hook (re-bound per GameState via key_handlers)
         drive the whole app; engine.machine.state is the one
         authoritative answer to "what's on screen". Direction keys
         (arrows+WASD), P/Q, and cheat keys F1-F5 all wired in
         handle_playing_key; game-over/victory -> name entry ->
         submit_name -> highscores reload chained end to end.)

--------------------------------------------------------------------------------
2. CONFIG MODULE (pacman/config.py)                                    owner: A (acombier)
--------------------------------------------------------------------------------
[x] 2.1  Comment stripping: remove lines starting with '#' before
         json.loads (comment lines blanked, not removed, so JSON error
         line numbers still match the original file)
[x] 2.2  Define GameConfig dataclass with all keys + safe defaults:
         highscore_filename, level[] (width/height per level, >= 10 levels),
         lives=3, pacgum=42, points_per_pacgum=10,
         points_per_super_pacgum=50, points_per_ghost=200, seed=42,
         level_max_time=90
[x] 2.3  Validation & clamping: wrong type -> default, out of range ->
         clamped to bounds + log a clear message, then CONTINUE
         (NOTE: bool rejected explicitly — bool is a subclass of int)
[x] 2.4  Unknown keys -> ignored (with a log line)
[x] 2.5  Handle: missing file, unreadable file, empty file, non-JSON,
         non-object root — fatal cases raise ConfigError (caught by the
         pac-man.py top-level guard, task 1.2), no traceback
[x] 2.6  Enforce minimum maze size (15x10 min, 41x41 max; fewer than 10
         levels -> padded with default 21x15)
[x] 2.7  Write config.json (commented example) shipped at repo root
[x] 2.8  pytest: comments stripped, bad types clamped, missing keys
         defaulted, unknown keys ignored, empty/corrupt file handled
         (tests/test_config.py, green 2026-07-17)
[x] 2.9  DEFENSE DRILL: prepare 5+ evil config files (evaluator WILL swap
         the config) and test each one
         (5 files in tests/evils_configs/ + parametrized pytest asserting
         GameConfig-or-ConfigError, never a traceback)

--------------------------------------------------------------------------------
3. MAZE LOADER (pacman/maze_loader.py) — anti-corruption layer         owner: A (acombier)
--------------------------------------------------------------------------------
[x] 3.1  Only module importing mazegenerator; call
         MazeGenerator(size=(w,h), perfect=False, seed=...) — the real API,
         NOT the package README (which is wrong)
         (real API probe-verified on 2026-07-16: maze[y][x], values 0-15,
         seed reproducible, wall symmetry OK)
[x] 3.2  Normalize coordinates once: everything downstream uses (x, y);
         grid indexed maze[y][x]; wall bitmask N=1 E=2 S=4 W=8
         (frozen dataclass Maze, tuple grid -> immutable; _normalize
         revalidates shape + cell bounds, never trusts the package)
[x] 3.3  Raise sys.setrecursionlimit proportionally to width*height BEFORE
         generating (w*h*4 + 1000; package uses recursive DFS)
[x] 3.4  try/except RecursionError + generic Exception -> MazeError with
         a clean message ("if the generator fails, handle cleanly")
[x] 3.5  Expose helpers: can_move(x, y, direction), is_solid(x, y),
         corridors(), nearest_corridor(), center(), corners()
[x] 3.6  shortest_path NOT used by our design -> nothing to handle
         (ghost AI will use our own BFS on the normalized Maze)
[x] 3.7  seed semantics: level 1 = fixed config seed (42);
         levels 2+ -> RANDOM_SEED constant (0 = system randomness)
[x] 3.8  pytest: seed reproducibility, wall symmetry between neighbors,
         value-15 cells excluded, garbage grids -> MazeError
         (tests/test_maze_loader.py, green 2026-07-17)

--------------------------------------------------------------------------------
4. HIGHSCORES (pacman/highscores.py)                                   owner: A (acombier)
--------------------------------------------------------------------------------
[x] 4.1  Storage: JSON file (path from config highscore_filename);
         document the "why JSON" choice in README (-> task 13.6)
[x] 4.2  Load at game start with context manager; missing or corrupt
         file -> start with empty list, log message, no crash
[x] 4.3  Validate entries: name max 10 chars, alphanumeric + spaces only;
         score = non-negative integer; drop invalid entries
[x] 4.4  Keep top 10 only, sorted descending (name + score)
[x] 4.5  Save at game end (context manager, atomic-ish write:
         write to .tmp then os.replace — old file intact on crash)
[x] 4.6  pytest: corrupt file, name validation, top-10 trimming,
         sort order, round-trip load/save
         (tests/test_highscores.py, green 2026-07-17)

--------------------------------------------------------------------------------
5. RENDERER SANDBOX (pacman/ui/renderer.py) — MLX (subject update)     owner: Sloubiat
--------------------------------------------------------------------------------
[x] 5.1  MLX window init (pacman/ui/mlx_window.py: MlxWindow — generic,
         reusable buffer/hooks wrapper, no game knowledge), cell-size
         computation from maze dimensions (MazeRenderer.load)
[x] 5.2  Draw maze walls from the bitmask (line per wall bit) — done in
         MazeRenderer._draw. value-15 cells filled with a distinct
         blue (_SOLID_COLOR) plus a white outline (_WALL_COLOR) — the
         "42" pattern IS visually called out (2026-07-23 check)
[x] 5.3  Draw the player (position + facing direction)
         (_draw_player, direction-aware; smooth inter-cell motion via
         the _Glide helper, see 5.5 note)
[x] 5.4  Keyboard input: arrows AND WASD, 4-direction movement,
         corridor-only (uses maze_loader.can_move)
         (pac-man.py _DIRECTION_KEYS -> engine.turn(); corridor
         legality enforced in Player/engine, not the renderer)
[x] 5.5  Grid-to-pixel conversion: cell_w/cell_h merged into a single
         _cell_size (forces square cells regardless of maze aspect
         ratio), computed once in MazeRenderer.load, reused by
         _draw_walls/_draw_dot/_draw_player/_draw_ghosts
         (DECISION: kept as a MazeRenderer instance concern rather
         than a standalone shared helper -- only the renderer needs
         it -> README/defense talking point if asked "why not a free
         function". _Glide class added on top for smooth per-tick
         interpolation between cells, driven by sync_tick()/
         set_tick_progress(), player_pace/ghost_pace from engine.py's
         PLAYER_MOVE_PERIOD/FRIGHTENED_MOVE_PERIOD.)
[x] 5.6  Playable sandbox milestone: walk a maze on screen
         (superseded by the full game loop being wired, 3c31dff
         2026-07-29 "link ui to engine" onward)
[x] 5.7  Draw pellets, super-pellets (_draw_pellets/_draw_dot, wired
         from pac-man.py via load_pellets, 2026-07-23); ghosts
         (_draw_ghosts/_draw_ghost_body/_draw_ghost_eyes -- flat
         per-ghost color, state read live off Ghost.state so it
         changes color the instant a ghost turns FRIGHTENED/EATEN,
         no extra bookkeeping needed) and HUD (_draw_hud, see 9.2)
         all done and wired in pac-man.py's frame() / sync_maze_view()

--------------------------------------------------------------------------------
6. PELLETS & SCORING (pacman/entities/pellets.py)                      owner: A (acombier)
--------------------------------------------------------------------------------
[x] 6.1  Pacgum placement in most corridors (exclude value-15 cells,
         player start cell, corners reserved for super-pacgums)
         (seeded RNG -> reproducible layout; 'pacgum' config key = count,
         clamped to available cells; done 2026-07-17)
[x] 6.2  Super-pacgums in the 4 corners of the maze
[x] 6.3  Consumption on player contact -> +points_per_pacgum /
         +points_per_super_pacgum
         (DONE 2026-07-27: engine inc 2 _eat_pellet maps PelletType ->
         config points; tested in tests/test_engine.py)
[x] 6.4  Super-pacgum triggers frightened mode on all ghosts (timer)
         (DONE 2026-07-27: engine inc 3 _eat_pellet SUPER branch calls
         ghost.frighten(to_ticks(FRIGHTENED_SECONDS)); tested)
[x] 6.5  Win condition: level complete when all pacgums eaten
         (all_eaten(); DECISION: super-pacgums count too -> README)
[x] 6.6  Score rule: score NEVER decreases; carries over between levels
         (DONE 2026-07-27: only `+=` in the engine, reset only in
         start_game(); _advance_level never touches score; carry-over
         tested in tests/test_engine.py)
[x] 6.7  pytest: placement rules, consumption, win detection
         (tests/test_pellets.py, 8 tests, green 2026-07-17)

--------------------------------------------------------------------------------
7. PLAYER & GHOSTS (pacman/entities/)                                  owner: A (acombier)
--------------------------------------------------------------------------------
[x] 7.1  Player class: position, direction, lives (from config),
         movement validation, respawn at maze center
         (buffered-turn design: direction + wanted; step() is
         time-free, the engine owns the cadence; tests green +
         committed 2026-07-20)
[x] 7.2  Ghost class with state machine: CHASE / FRIGHTENED / EATEN
         (DECISION: entities count time in engine TICKS, never
         seconds — engine converts via time stdlib, MLX has no clock;
         one shared `timer` field for both timed states -> README.
         tests/test_ghost.py, 7 tests, green + committed 2026-07-21)
[~] 7.3  Ghost AI strategies (small, explainable-in-2-min classes):
         [x] strategy A: direct chase (BFS distance to player)
             (ghost_ai.py: ONE bfs_distances() map computed from the
             player per tick, shared by all ghosts; ChaseStrategy
             picks the legal neighbour minimizing it, N/E/S/W
             tie-break; tests green + committed 2026-07-21)
         [x] strategy B: random at intersections
             (RandomStrategy: random among legal non-reverse moves ==
             straight in corridors, real choice only at intersections;
             U-turn only in dead ends; needs Ghost.direction field +
             OPPOSITE in maze_loader; tests green 2026-07-22)
         [ ] strategy C: ambush (target ahead of player) — OPTIONAL,
             decide before defense whether 2 strategies + flee are
             enough variety (document the choice in README either way)
[x] 7.4  Frightened behavior: flee from player (maximize distance)
         (FleeStrategy: mirror of ChaseStrategy, max instead of min,
         same N/E/S/W tie-break; greedy/local so a ghost can trap
         itself in a dead end — accepted trade-off -> README;
         tests green 2026-07-22)
[x] 7.5  Eaten: ghost returns to / respawns at its corner after a
         configurable delay (~5-10 s)
         (DONE 2026-07-27: engine inc 3 wires get_eaten with
         to_ticks(RESPAWN_SECONDS)=7 s; ghost.tick() counts down in
         _move_ghosts; DECISION: no arcade "eyes travel home"
         animation -> README)
[x] 7.6  4 ghosts spawn one per corner; player spawns middle
         (already done by build_level, task 8.2; confirmed by
         tests/test_level.py)
[x] 7.7  Collisions: ghost touches player (not frightened) -> lose a life,
         respawn player center; game over at 0 lives
         (DONE 2026-07-27: engine inc 3 _handle_collisions +
         _lose_life; round reset = every ghost send_home; tested)
[x] 7.8  Player eats frightened ghost -> +points_per_ghost, ghost -> EATEN
         (DONE 2026-07-27: engine inc 3 _handle_collisions; tested)
[x] 7.9  pytest: state transitions, BFS chase step, respawn timer,
         collision outcomes (entity side DONE; collision outcomes
         belong to the engine and move to task 8 tests)
         (player: tests/test_player.py, 7 tests on a handcrafted 3x3
         "plus" maze, green 2026-07-20; ghost: tests/test_ghost.py
         covers state transitions + respawn timer, tests/test_ghost_ai.py
         covers BFS map + chase step + convergence, green 2026-07-21;
         flee + random strategies covered 2026-07-22 (76 tests total) —
         the enum import-typo incident is now test-covered, lint alone
         cannot catch import typos (it happened AGAIN with OPPOSITE);
         remaining: collision outcomes -> engine, task 8)

--------------------------------------------------------------------------------
8. GAME ENGINE & LEVELS (pacman/game/)                                 owner: both (pair)
--------------------------------------------------------------------------------
[x] 8.1  states.py: state enum MENU / PLAYING / PAUSED / GAME_OVER /
         VICTORY (+ name-entry state) and transition rules
         (code done 2026-07-22: GameState enum with 8 screens incl.
         HIGHSCORES / INSTRUCTIONS / NAME_ENTRY shared by win & lose;
         TRANSITIONS dict of frozensets, StateMachine.transition_to
         raises ValueError on illegal moves; DECISION: no EXIT state,
         quitting = engine `running` flag -> README;
         tests/test_states.py, 6 tests, green 2026-07-22 — 82 total)
[x] 8.2  level.py: one level = maze + pellets + entities + countdown
         timer (level_max_time from config)
         (Level dataclass + build_level factory — seed policy lvl 1
         fixed / lvl 2+ random, 4 ghosts with cycling personalities
         Chase/Random (5th-ghost drill ready), lives passed in for
         carry-over, ticks countdown; tests/test_level.py, 7 tests,
         green 2026-07-22 — 93 total)
[x] 8.3  engine.py: main loop (events -> update -> render), fixed
         timestep / clock
         (ENGINE SIDE COMPLETE 2026-07-27, incs 1-4 below; the
         events/render halves live in ui/ + pac-man.py — task 1)
         (increment 1 IN PLACE 2026-07-23, DELEGATED file — defense
         material, acombier must re-read: to_ticks() seconds->ticks,
         TICKS_PER_SECOND=8, monotonic-clock accumulator in update(),
         MAX_TICKS_PER_UPDATE=4 clamp so an OS freeze pauses instead
         of fast-forwarding, accumulator drained outside PLAYING so
         pause never builds catch-up ticks.
         Increment 2 IN PLACE 2026-07-27, DELEGATED (acombier re-read
         pending): turn() buffers keys only while PLAYING; _tick =
         timer -> player.step -> _eat_pellet -> completion check;
         helpers take `level` as param (mypy Optional narrowing);
         _eat_pellet maps PelletType -> config points (frightened
         trigger deferred to inc 3); _advance_level = next level with
         lives carry-over, or VICTORY after the last one.
         tests/test_engine.py: 10 tests with a FakeClock monkeypatched
         over time.monotonic (cadence, clamp, pause drain, scoring,
         carry-over, victory) — 103 total, lint green.
         Increment 3 IN PLACE 2026-07-27, DELEGATED (acombier re-read
         pending): _move_ghosts = one shared BFS map per tick,
         ghost.tick() timers, FRIGHTENED -> engine-owned FleeStrategy,
         CHASE -> per-ghost personality, EATEN waits at home;
         _handle_collisions runs TWICE per tick (after player move,
         after ghost moves) so nobody phases through anyone;
         _lose_life = player respawn + every ghost send_home (new
         Ghost method) + GAME_OVER at 0 lives; ghosts move at player
         speed (1 cell/tick) -> README. 5 new tests (108 total).
         Increment 4 IN PLACE 2026-07-27, DELEGATED (acombier re-read
         pending): timer expiry decision wired in _tick (see 8.5);
         toggle_pause / quit_to_menu / enter_name_entry /
         submit_name public API for the UI; submit_name re-validates
         via highscores.is_valid_name (engine = last line of defense
         before the file) then load -> add_score -> save -> MENU.
         5 new tests (113 total), lint green.)
[x] 8.4  Level progression: >= 10 levels; level 1 fixed seed, next levels
         random; score and lives carry over
         (DONE 2026-07-27: _advance_level + build_level seed policy +
         carry-over, tested in test_engine.py + test_level.py)
[x] 8.5  Timer expiry behavior: DECIDE it (e.g., lose a life / restart
         level) and DOCUMENT it in README
         (DECISION 2026-07-27: timeout costs one life, the timer
         refills, round resets (player center, ghosts home) — at 0
         lives it naturally becomes game over. Still to DOCUMENT in
         README, task 13)
[x] 8.6  Pause / resume during gameplay
         (DONE 2026-07-27: Engine.toggle_pause + quit_to_menu;
         accumulator already drained while paused (inc 1); tested)
[x] 8.7  Game end (win or lose): show final score -> name prompt ->
         save highscore -> back to main menu
         (ENGINE SIDE DONE 2026-07-27: enter_name_entry + submit_name
         (save + back to MENU), tested incl. invalid-name refusal.
         UI SIDE DONE 2026-07-29 (3c31dff "link ui to engine"):
         GameOverScreen/VictoryScreen -> engine.enter_name_entry() ->
         NameEntryScreen -> engine.submit_name() -> on success,
         highscores_screen reloaded from disk so the board is fresh
         next time it's opened. Dev-only KEY_G/KEY_V shortcuts removed.)
[x] 8.8  Keep engine + entities graphics-free (unit-testable); only
         ui/ touches the MLX wrapper; timing via stdlib `time`
         (MLX has no clock)
         (HOLDS 2026-07-27: engine imports only stdlib random/time +
         pacman data modules; the whole suite runs headless on macOS)

--------------------------------------------------------------------------------
9. UI SCREENS & HUD (pacman/ui/menus.py)                               owner: Sloubiat
--------------------------------------------------------------------------------
[x] 9.1  Main menu: Start Game / View Highscores (top 10 + names) /
         Instructions (controls + rules + cheat keys) / Exit
         (MainMenu class: all 4 options, Up/Down/Enter navigation,
         selected-button highlight, all wired in pac-man.py.
         HighscoresScreen: scores injected via load(), same pattern
         as MazeRenderer.load/_EndScreen.set_score; draws
         rank+name+score per entry (or a placeholder when empty);
         back to menu on Enter/Escape; refreshed after every
         submit_name() so a fresh result shows up immediately.
         InstructionsScreen.LINES now lists the 5 cheat keys
         (F1-F5, added with cheat mode wiring, c14faf5 2026-08-09) —
         the gap noted on 2026-07-27 is closed.)
[x] 9.2  In-game HUD, always visible: score, lives, level, remaining time
         (_draw_hud in renderer.py: score/level/seconds_left pushed
         once per frame via maze_renderer.set_hud(...) in
         pac-man.py's frame() (no single mutable object to read them
         from at draw time); lives are read live off
         self._player.lives instead, since Player already carries
         that state. Remaining time text turns red under
         _HUD_WARNING_SECONDS.)
[x] 9.3  Pause menu: Resume / Return to main menu
         (PauseScreen (_OptionMenu subclass): reachable only from
         PLAYING via P (engine.toggle_pause()); Resume calls
         toggle_pause() again, Return to Main Menu calls
         engine.quit_to_menu())
[x] 9.4  Game Over screen: final score + name entry prompt
         (GameOverScreen (_EndScreen): title + final score + "press
         Enter"; on confirm -> engine.enter_name_entry() ->
         NameEntryScreen -> engine.submit_name() -> saved to
         highscores.json -> back to MENU. Dev-only shortcut removed,
         chain done 2026-07-29 "link ui to engine".)
[x] 9.5  Victory screen: final score + congratulatory message + name entry
         (VictoryScreen (_EndScreen): same chain as 9.4, plus the
         congratulatory MESSAGE; dev-only shortcut removed.)
[x] 9.6  Name input widget: live validation (max 10 chars, alphanumeric
         + spaces only)
         (NameEntryScreen done: reuses highscores.is_valid_name +
         MAX_NAME_LENGTH so the same rule governs typing and storage
         — single source of truth; backspace, live refresh, Enter
         only confirms a valid name; wired after "Start Game" in
         pac-man.py today as a placeholder flow, reusable as-is once
         chained after 9.4/9.5, 2026-07-27)

--------------------------------------------------------------------------------
10. CHEAT MODE (in engine, keys documented)                            owner: both (pair)
-------------------------------------------------------------------------------
          NOTE 2026-08-09 (c14faf5 "add cheat"): engine side DONE for
          10.1-10.5 since 2026-07-27 (Cheats dataclass + cheat_*
          methods, reset by start_game, 6 tests); keys are now bound
          too -- _CHEAT_KEYS in pac-man.py maps F1-F5 to the Engine.
          cheat_* methods, read in handle_playing_key so cheats only
          fire during PLAYING. InstructionsScreen.LINES lists all 5.
          Only 10.6 (copy the same list into README.md) is still open.
[x] 10.1  Invincibility toggle (no life lost on ghost contact)
          (cheat_toggle_invincibility: _handle_collisions skips the
          CHASE branch; frightened ghosts stay edible)
[x] 10.2  Level skip (instantly win current level)
          (cheat_level_skip reuses _advance_level: carry-over and
          VICTORY on last level behave like a real clear)
[x] 10.3  Ghost freeze (ghosts stop moving)
          (cheat_toggle_ghost_freeze: _tick skips the ghost half;
          DECISION: their timers freeze too -> README)
[x] 10.4  Extra lives (add lives)
          (cheat_extra_life: +1 life, PLAYING only)
[x] 10.5  Speed boost (player moves faster)
          (cheat_toggle_speed_boost: 2 player steps per tick, eating
          between steps so no pellet is jumped over)
[~] 10.6  Document all cheat keys in README + in the Instructions screen
          (must genuinely help the reviewer test everything)
          (Instructions screen DONE (F1-F5 listed with their effect,
          see above). README.md still has the two TODO markers left
          by the 2026-07-28 draft ("confirm key map" / "document
          activation keys") -- keys are stable now (F1-F5, see
          pacman/ui/keys.py), just needs the markers replaced with
          the final table -> task 13.3)

--------------------------------------------------------------------------------
11. PACKAGING & DEPLOYMENT (Itch.io)                                   owner: A (acombier)
--------------------------------------------------------------------------------
[ ] 11.0  NOTE (MLX update): MLX is a native C/X11 lib -> the package
          must bundle its sources + a build step (or a prebuilt .so),
          target is Linux/X11; document the requirements (X11 dev
          libs) on the Itch.io page and in the README
[ ] 11.1  Choose + write the packaging script/spec (e.g., PyInstaller or
          zipped source + install script) — kept at repo root
[ ] 11.2  Include minimal in-package instructions (controls, options,
          config file usage)
[ ] 11.3  Test the package on a clean machine/venv (fully functional)
[ ] 11.4  Create Itch.io page: free, unlisted build; upload
[ ] 11.5  Rehearse REGENERATING the package live (asked during review)

--------------------------------------------------------------------------------
12. PROJECT MANAGEMENT (project-management/)                           owner: both
--------------------------------------------------------------------------------
          NOTE 2026-07-28: 6 documents written (AI-drafted from
          TODO.txt/git history, to be team-reviewed): timeline.md,
          technical-choices.md, risk-analysis.md,
          team-organization.md, acceptance-test-plan.md,
          blocking-points.md
[x] 12.1  Timeline: Gantt or Kanban (set up early, keep it alive)
          (timeline.md: milestone plan + dated actuals; the live
          Kanban is TODO.txt itself)
[~] 12.2  Progress tracking vs plan (update regularly, keep the deltas)
          (status table in timeline.md — must be refreshed until
          submission)
[x] 12.3  Project analysis & technical choices document
[x] 12.4  Risk analysis + mitigation (e.g., mazegenerator quirks, config
          swap at defense, pygame packaging, uv missing on the review
          machine -> pip fallback)
[x] 12.5  Team organization: who did what, how decisions were made,
          how issues were handled (2-person split)
[~] 12.6  Acceptance test plan: features tested, bugs found/fixed
          (written; the manual Linux checklist must be RUN before
          the defense and after packaging)
[x] 12.7  Summary of blocking points / conflicts

--------------------------------------------------------------------------------
13. README.md (English, at repo root)                                  owner: Sloubiat
--------------------------------------------------------------------------------
          NOTE 2026-08-17: task 1 (pac-man.py wiring) and task 10
          (cheat keys) both landed since the 2026-07-28 draft, so the
          two TODO markers left in README.md are now unblocked —
          just need the final table pasted in, no more code to wait
          on. Sloubiat still needs to fill his own AI-usage bullet.
[x] 13.1  First line, in italics: "This project has been created as part
          of the 42 curriculum by <login1>, <login2>."
[x] 13.2  Description section (goal + brief overview)
[~] 13.3  Instructions section (install, run, controls, Makefile targets)
          (controls table already correct; still has
          "<!-- TODO(task 1): confirm the final key map -->" and
          "<!-- TODO(task 10.6): document the activation keys -->"
          markers to remove now that pac-man.py/keys.py are final —
          copy the 5-row cheat table from InstructionsScreen.LINES)
[~] 13.4  Resources section: references + HOW AI WAS USED (which tasks,
          which parts) — mandatory per subject ch. II/IX
          (everything written except one marker:
          "<!-- TODO(sloubiat): describe your AI usage for the ui/
          modules -->" under the UI layer bullet — Sloubiat's to fill)
[x] 13.5  Configuration section: every key, type, default, clamping rules
[x] 13.6  Highscore section: how it works + why this storage choice
[x] 13.7  Maze Generation section: how the A-Maze-ing package is used
          (real API, seed policy, error handling)
[x] 13.8  Implementation section: technical summary
[x] 13.9  General Software Architecture section: modules, classes,
          relationships (small diagram)
[x] 13.10 Project Management section: overview + link to
          project-management/

--------------------------------------------------------------------------------
14. QUALITY GATES (continuous, before every commit)                    owner: both
--------------------------------------------------------------------------------
          NOTE 2026-08-17: `make lint` and `make test` currently both
          green (126 tests). Found and fixed a real regression while
          auditing this file: the renderer refactor around
          2026-07-27/28 renamed MazeRenderer._draw -> _draw_walls,
          but tests/test_ui_headless.py still called the old name —
          broke both `make lint` (mypy attr-defined error) and 3
          pytest cases. This is exactly the kind of drift these
          "continuous" checkboxes exist to catch; re-run `make lint`
          and `make test` after every merge, don't trust this file's
          last-known-green state.
[x] 14.1  flake8 clean on the whole repo
[x] 14.2  mypy clean with the mandatory flags (aim for --strict)
          (not yet run with --strict / lint-strict — only the
          mandatory flag set from the subject; try lint-strict before
          the defense and see how far off it is)
[x] 14.3  Type hints + Google-style docstrings on every function/class
          (holds under --disallow-untyped-defs --check-untyped-defs;
          spot-check docstring quality on the oldest files before
          the defense, they were written earliest and reviewed least)
[x] 14.4  try/except on anything that can fail; context managers for
          all file access
[x] 14.5  pytest suite green (config, maze adapter, highscores, entities)
          (126 tests green as of 2026-08-17; test_config.py 17,
          test_engine.py 29, test_ghost.py 7, test_ghost_ai.py 18,
          test_highscores.py 11, test_level.py 7, test_maze_loader.py
          9, test_pellets.py 8, test_player.py 7, test_states.py 6,
          test_ui_headless.py 7)

--------------------------------------------------------------------------------
15. DEFENSE PREPARATION                                                owner: both
--------------------------------------------------------------------------------
[ ] 15.1  Each teammate can explain EVERY function (cross-review sessions)
[ ] 15.2  Config-swap drill: run the game against the evil configs (2.9)
[ ] 15.3  Recode drills (few-minute live changes), e.g.:
          - add a 5th ghost
          - change the ghost respawn delay
          - store the date in the highscore file
          - change a HUD element / a color
          - add a new config key with validation
[ ] 15.4  Rehearse justifying architecture choices (anti-corruption
          layer, pygame-free logic, state machine, ghost strategies)
[ ] 15.5  Rehearse regenerating the Itch.io package live
[ ] 15.6  Double-check file names in the repo before submission

================================================================================
MILESTONES (suggested)
  M1: 0-1 done, lint green                    (setup)              [x] DONE
  M2: 2-4 done + tests                        (data layer)         [x] DONE
  M3: 5 done                                  (playable sandbox)   [x] DONE
  M4: 6-8 done                                (full game loop)     [x] DONE
  M5: 9-10 done                               (UI + cheats)        [~] 10.6 open (README doc only)
  M6: 11-13 done                              (ship it)            [ ] 11 packaging not started; 13.3/13.4 two markers left
  M7: 14-15 continuously + final rehearsal    (defense-ready)      [~] 14 green today, 15 not started

  STATUS AS OF 2026-08-17: the game is fully playable end to end
  (menu -> play -> pause -> game over/victory -> name entry ->
  highscores -> menu) with all 5 cheats bound. What's actually left:
  - README: fill 3 TODO markers (key map, cheat keys, Sloubiat's AI
    usage bullet) -- task 13.3/13.4, no code involved
  - Packaging to Itch.io -- task 11, entirely untouched, biggest
    remaining block of work
  - project-management docs: re-read as a team, don't just trust the
    AI-drafted version (12.2, 12.6)
  - Defense prep -- task 15, entirely untouched (cross-review,
    config-swap drill, recode drills, packaging rehearsal)

TEAM SPLIT (agreed):
  acombier: 2, 3, 4 (data layer), then 6, 7 (entities), 11 (packaging)
  Sloubiat: 1 (entry point), 5 (rendering sandbox), 9 (menus/HUD),
            13 (README)
  Pair:     0 (kickoff), 8 (engine), 10 (cheats), 12, 14, 15
================================================================================
